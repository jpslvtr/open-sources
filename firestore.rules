rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only Cloud Functions can read/write - no client access needed
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
